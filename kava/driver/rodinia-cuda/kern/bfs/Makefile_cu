#
# The following must be provided:
#

# An executable name
EXECUTABLE	:= bfs
# Cuda source files (compiled with nvcc)
CUDA_FILES	:= bfs_cuda_kernels.cu

include ../util/cuda.mk

dump:
	gcc graph_dumper.c -o graph_dumper
