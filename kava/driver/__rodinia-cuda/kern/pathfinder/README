Modified by Hangchen Yu 02/2017 for GDEV and CUDA 4.2
Modified by Hangchen Yu 01/2020 for Kernel AvA

To compile the program:

nvcc -cuda dynproc.cu
nvcc -o dynproc dynproc.cu.cpp 

Usage: dynproc row_len col_len pyramid_height

Compile CUDA kernel

```
make -f Makefile_cu
```

Run pathfinder

```
make
sudo insmod pathfinder.ko rows=100 cols=100000 pyramid_height=20 cubin_p="/project/kava/driver/kava-benchmarks/rodinia-cuda/kern/pathfinder"
```
