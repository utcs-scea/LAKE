The original BFS CUDA code was obtained from Pawan Harish and P. J. Narayanan at IIIT, 
who have given us permission to include it as part of Rodinia under Rodinia's license.

Dump input graph file:

```
make -f Makefile_cu dump
./graph_dumper /project/rodinia-data/bfs/graph4096.txt
```

Compile CUDA kernel

```
make -f Makefile_cu
```

Run bfs

```
make
sudo insmod bfs.ko input_p="/project/rodinia-data/bfs" input_f="graph4096.bin" cubin_p="/project/kava/driver/kava-benchmarks/rodinia-cuda/kern/bfs"
```
